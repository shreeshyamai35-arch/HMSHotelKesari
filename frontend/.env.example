# ── API base URL ─────────────────────────────────────────────────────────────
# Leave empty (or omit) when frontend and backend share the same domain on Vercel.
# Only set this if you run the frontend on a different origin than the backend.
# VITE_API_BASE_URL=
